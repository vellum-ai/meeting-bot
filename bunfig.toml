# Scope the default `bun test` run to the plugin's own suites. The vendored
# meet-join subsystem under meet/ carries its own tests (including Docker and
# live-meeting e2e suites that need a browser stack); run those explicitly with
# `bun test meet/...` when working on the vendored code.
[test]
root = "src"
