node_modules/
bun.lock
*.log

# Plugin runtime state written into the plugin directory by the host.
config.json
data/
.disabled
install-meta.json

# Compiled app output (built by the host from apps/<name>/src/).
apps/*/dist/
